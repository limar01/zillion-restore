# 🚨 INCIDENT + RECOVERY CHECKLIST (2026-09-09 ~02:00 PST)

## Ano ang nangyari (honest postmortem)
- Deploy ng lahat ng CR fixes: SUCCESS (beacon v3, supervisor v4, cf_retry v3, adb_watch v2, zg v1.1, .profile guard, worker ver fix — lahat naka-install at naka-verify).
- **Ang aking mali:** sa rolling-restart kill loop, ang case pattern (`*zillion_pw/supervisor.sh*` atbp.) ay **tumama sa SARILING exec shell** (ang script text mismo ay naglalaman ng mga string na iyon) → pinatay ng script ang sarili bago pa nakapag-run ng `start_all.sh`.
- Resulta: supervisor + worker + beacon + cf_retry + fallback_retry + zg = **patay**. Ang cf at ssh process ay buhay pero dead ang backend (zg) → walang matinong channel.
- **Phone = DARK.** Walang tunnel, walang MQTT worker, walang beacon.

## Recovery (Boss, 1 action lang — PILI)
- **Option A (mabilis): buksan ang Termux app** (1 tap) → login shell → `.profile`/`.bashrc` guard → `start_all.sh` → supervisor v4 (may AB_CMD_TIMEOUT=120) → lahat serbisyo babangon. Ito rin ang LIVE TEST ng F6 guard.
- **Option B: i-reboot ang phone** → Termux:Boot chain (verified kanina) → same outcome.

## Pagkatapos ng recovery — i-verify ko (hindi pa nagagawa)
1. Supervisor v4 + lock test (`start_all` ulit → "lock held", 1 supervisor lang)
2. Worker env: AB_CMD_TIMEOUT=120 + ver 4.5
3. Beacon v3: retained URL + `healthy` flag + `url_age` (F1 proof)
4. zg v1.1: replay test (same id → 409) + stale ts test (→ 401) + timeout clamp (F5 proof)
5. cf_retry v3: cf.log rotation (F7 proof)
6. E2E: resolve → exec → ADB state
7. I-sync ang core v1.3.2 (bagong embedded clients + bagong doktrina: **kill-loop self-match rule**)

## Bagong doktrina (idagdag sa core v1.3.2)
- **KILL-LOOP SELF-MATCH RULE:** sa lahat ng ad-hoc kill loop sa exec: (a) anchored patterns LANG (`^python3 .*zg\.py$` style), (b) `[ "$p" != "$$" ]` + i-exclude ang sariling process tree (parent pgid), (c) huwag ilagay ang target string sa case pattern na lalabas sa sariling cmdline. Ang incident na ito ang proof kung bakit.
